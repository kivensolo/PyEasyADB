# EasyPyADB → Compose Multiplatform 迁移进度

> 配套文档：[`01_模块映射表.md`](./01_模块映射表.md) | [`02_分阶段迁移计划.md`](./02_分阶段迁移计划.md)

---

## 总览

| 阶段 | 状态 | 完成日期 | 提交 |
|---|---|---|---|
| P0 脚手架搭建 | ✅ 已完成 | 2026-06-29 | `e659967` |
| P1 Core 层迁移 | ✅ 已完成 | 2026-06-30 | — |
| P2 主题与设计系统 | ✅ 已完成 | 2026-06-30 | — |
| P3 主窗口骨架 | ✅ 已完成 | 2026-07-01 | — |
| P4 设备树 + 命令树 | ✅ 已完成 | 2026-07-03 | — |
| P5 功能区 + 自定义行为 | ⏳ 未开始 | — | — |
| P6 控制台 + Live Logcat | ⏳ 未开始 | — | — |
| P7 对话框批量迁入 | ⏳ 未开始 | — | — |
| P8 Scrcpy 独立窗口模式 | ⏳ 未开始 | — | — |
| P9 打包分发 | ⏳ 未开始 | — | — |
| P10 Scrcpy 嵌入模式（可选） | ⏳ 未开始 | — | — |
| P11 老数据迁移工具 | ⏳ 未开始 | — | — |

---

## P0 — 脚手架搭建 ✅

> 完成日期：2026-06-29 | 提交：`e659967` | 分支：`dev/v2.0.0_cmp`

### 技术栈（已锁定，后续阶段请沿用）

| 组件 | 版本 | 说明 |
|---|---|---|
| Kotlin | 2.0.21 | 必须配套 Compose Compiler 插件 |
| Compose Multiplatform | 1.7.3 | JetBrains 官方稳定版 |
| Gradle | 8.10 | wrapper 已提交 |
| JDK | 17 LTS | 路径：`D:\jdks\jdk-17.0.13`（Windows） |
| kotlinx-coroutines | 1.9.0 | |
| kotlin-logging | 7.0.3 | 坐标 `io.github.oshai:kotlin-logging-jvm` |
| slf4j-simple | 2.0.16 | 运行时日志后端 |

### 模块结构（17 个）

```
cmp/
├── settings.gradle.kts          # 包含全部 17 个 include
├── build.gradle.kts              # 根脚本，4 个 plugin alias + jvmToolchain(17)
├── gradle.properties             # JDK 路径、缓存、并行编译配置
├── gradle/libs.versions.toml     # 版本目录
├── gradle/wrapper/               # gradle-wrapper.jar + properties
├── gradlew / gradlew.bat
│
├── platform/                     # KMP 模块（commonMain + desktopMain）
│   └── AppPaths expect/actual    # 跨平台路径解析（已有实现）
│
├── desktop-app/                  # Compose Desktop 入口
│   └── Main.kt                   # 启动类，含 slf4j 日志配置
│
├── core/                         # 7 个空壳模块（仅有 build.gradle.kts）
│   ├── adb/  apk/  config/  database/  device/  log/  util/
│
└── ui/                           # 8 个空壳模块（仅有 build.gradle.kts）
    ├── designsystem/  home/  devicelist/  functions/
    └── console/  logcat/  mirror/  dialogs/
```

### P0 验收清单

- [x] `cmp/` 目录结构完整（17 模块）
- [x] `./gradlew build` — BUILD SUCCESSFUL
- [x] `./gradlew run` — 弹出 "EasyADB" 窗口，显示 `EasyADB CMP v2.0.0 - P0 scaffolding`
- [x] 日志写入 `%LOCALAPPDATA%/EasyADB/logs/easyadb.log`
- [x] 根 `.gitignore` 已追加 Kotlin/Gradle 规则

### 已知坑点（P1 及后续阶段注意）

- **expect/actual Beta**：Kotlin 2.0.21 中 `expect/actual class` 仍是 Beta，需加 `-Xexpect-actual-classes` 编译器标志
- **日志初始化时序**：`System.setProperty` 必须在创建 logger **之前**调用，logger 必须是 `main()` 内的局部变量而非顶层变量
- **JDK 路径格式**：Gradle 在 Git Bash 下不识别 `/d/jdks/` 格式，必须用 `D:\\jdks\\jdk-17.0.13`
- **Compose Material 版本**：P0 使用 Material 2（`material`），P2 主题阶段计划迁 Material 3
- **Kotlin 2.0+ Compose 插件**：必须同时 apply `org.jetbrains.compose` 和 `org.jetbrains.kotlin.plugin.compose` 两个插件
- **Gradle 9.0 兼容**：当前有 deprecated feature 警告，不影响构建，Gradle 9.0 需处理

---

## P1 — Core 层迁移 ✅

> 完成日期：2026-06-30 | 分支：`dev/v2.0.0_cmp`

### 完成的工作

**Step 1 — 扩展 platform 模块：**
- `ProcessKiller` expect/actual（Windows: `taskkill /F /PID`，Unix: `kill -9`）
- `TerminalOpener` expect/actual（各平台启动终端）
- 添加 `-Xexpect-actual-classes` 编译器标志
- 将 `kotlinx-coroutines-core` 移至 `commonMain`

**Step 2 — `:core:log` 模块（7 文件）：**
- `LogLevel.kt` — 枚举 TRACE/DEBUG/INFO/WARN/ERROR/OFF
- `LogConfig.kt` — 配置 data class（maxBytes=4MB, backupCount=128）
- `RollingFileHandler.kt` — 自定义滚动文件 handler
- `AppLogger.kt` — 日志 facade + `getLogger()` 工厂方法
- `LogLevelMapper.kt` — `simpleNameToLevel`/`nameToLevel` 映射
- `LogConstants.kt` — `TIME_STAMP_PREFIX`、`LIVE_LOG_PREFIX` 等常量
- `LogUtils.kt` — `changeLogColor()`、`highlightLinkAddr()`、`buildTimeStamp()`

**Step 3 — `:core:util` 模块（5 文件）：**
- `ExecResult.kt` — data class 结果封装
- `ExecUtils.kt` — `suspend execCmd()` 使用 ProcessBuilder + OS 感知 shell
- `NetworkUtils.kt` — `isIpMatches()`、`parseIpPort()`
- `FileUtils.kt` — `calculateMd5()`、`clearDirectory()`
- `DateTimeUtils.kt` — `buildTimeStamp()`、`todayDate()`

**Step 4 — `:core:config` 模块（7 文件）：**
- `IniParser.kt` — 自定义 INI 解析器支持 `[Section]` 头
- `AppConfigManager.kt` — 单例加载 `AppConfig.ini`
- `CmdConfig.kt` — `CmdGroup`/`CmdItem`/`CmdSubGroup` data classes
- `FunctionTemplate.kt` — `FunctionTemplate`/`FunctionItem` data classes
- `MenuConfig.kt` — `MenuConfig`/`MenuAction` data classes
- `XmlConfigLoader.kt` — 3 个 XML 加载函数（cmdConfig/functionTemplates/menusUi）
- `AppPathsConfig.kt` — 路径常量对象

**Step 5 — `:core:database` 模块（3 文件）：**
- `DeviceRecord.kt` — data class（ip, port, alias, deviceInfo, active）
- `DbResult.kt` — 泛型结果封装
- `DbManager.kt` — SQLite 管理器，完整 CRUD，表结构与 Python 一致

**Step 6 — `:core:adb` 模块（6 文件）：**
- `ActionCmdParams.kt` — ADB 命令参数组装 + 宏替换
- `CmdExecutor.kt` — 命令执行器 + Flow 异步执行
- `LogcatStream.kt` — 连续 logcat 流 + LogcatFilter（级别/进程/关键字三级过滤）
- `ProcessUtils.kt` — `adb shell ps` 输出解析 + 过滤
- `AppListLoader.kt` — 异步加载应用列表（第三方/系统分类）
- `AdbExecutor.kt` — ADB 操作封装（connect/disconnect/screenshot/screenrecord）

**Step 7 — `:core:device` 模块（3 文件）：**
- `DeviceInfo.kt` — data class + `DeviceState` 枚举
- `DeviceAddress.kt` — 密封类（IpPort/Emulator/Serial）
- `DevicesWatcher.kt` — 协程 + Flow 替代 threading.Thread + pyqtSignal

**Step 8 — `:core:apk` 模块（2 文件）：**
- `ApkInfo.kt` — APK 信息 data class
- `ApkParser.kt` — 使用 aapt/apksigner 解析 APK

**Step 9 — 更新 desktop-app 入口：**
- `build.gradle.kts` 追加 7 个 core 模块依赖
- `Main.kt` 初始化顺序：日志 → 配置 → DB → DeviceWatcher
- 窗口文本更新为 `"EasyADB CMP v2.0.0 - P1 Core Layer active"`

### 关键设计决策

- **QThread → coroutine**：`LiveLogAdbThread` → `callbackFlow`，`DevicesWatcher` → `CoroutineScope` + `StateFlow`
- **pyqtSignal → Flow**：`changedSignal` → `devices: Flow<List<DeviceInfo>>`，`live_log_dump_signal` → `Flow<LogcatEntry>`
- **SQLite**：JDBC 直连（`org.xerial:sqlite-jdbc`），表结构与 Python 完全一致
- **INI 解析**：自写 `IniParser`（Java `Properties` 不支持 `[Section]`）
- **日志类型**：`LogLevel` 枚举替代 Python 的 `logging` 级别数字
- **仅 platform 模块使用 KMP**：EasyPyADB 目标单一（Windows 桌面工具），无需跨平台编译到 iOS/Android/Web。因此只将 `platform` 模块设为 `kotlin.multiplatform`（它需要 expect/actual 处理平台差异），其余所有模块（含 `desktop-app` 和 7 个 core 模块）保持 `kotlin("jvm")`，避免 KMP 带来的 expect/actual 声明、source set 层次结构等不必要的复杂度。

### 验收清单

- [x] `./gradlew build` — BUILD SUCCESSFUL
- [x] `./gradlew :core:log:build` — 日志模块独立编译通过
- [x] `./gradlew :core:util:build` — 工具模块独立编译通过
- [x] `./gradlew :core:config:build` — 配置模块独立编译通过
- [x] `./gradlew :core:database:build` — 数据库模块独立编译通过
- [x] `./gradlew :core:adb:build` — ADB 模块独立编译通过
- [x] `./gradlew :core:device:build` — 设备模块独立编译通过
- [x] `./gradlew :core:apk:build` — APK 模块独立编译通过
- [x] 入口初始化顺序正确：日志 → 配置 → DB → DeviceWatcher

### 新增坑点

- **LogLevel 类型**：`LogLevelMapper` 返回 `LogLevel` 枚举而非 `Int`，使用 `LogLevel.value` 比较级别
- **AppLogger 实例**：`AppLogger` 是实例类，需通过 `companion object.getLogger()` 工厂方法创建
- **Kotlin 2.0+ DSL**：`kotlinOptions { }` 已废弃，改用 `compilerOptions { }`
- **KMP 层次模板**：显式 `dependsOn(commonMain)` 会阻止自动层次模板应用，KMP 2.0+ 自动处理

---

## P2 — 主题与设计系统 ✅

> 完成日期：2026-06-30 | 分支：`dev/v2.0.0_cmp`

### 完成的工作

**`ui/designsystem` 模块创建（4 文件）：**

- **`Color.kt`** — `LogColor` 对象（Debug=#388E3C, Info=#263238, Warn=#B07805, Error=#BF360C, Stamp=#005AC7），`EasyAdbColors` 调色板（Primary/Surface/Background/输入框/按钮/状态/对话框等颜色，全部从 Python QSS 样式提取）
- **`Typography.kt`** — `EasyAdbTypography`，平台感知字体族（Windows: SansSerif → Microsoft YaHei，macOS: SansSerif → PingFang SC，Linux: SansSerif → Noto Sans CJK SC），字号从 Python 字体函数提取（标题 26sp、正文 13sp、标签 10sp、按钮 10sp 等）
- **`Theme.kt`** — `EasyAdbTheme` composable，封装 `MaterialTheme`（light/dark 调色板），集成自定义颜色和排版
- **`Icons.kt`** — `AppIcons` 对象，17 个 `res/icons/*.png` 映射到 Material Icons（`Icons.Filled.*`），使用 `compose.materialIconsExtended` 提供扩展图标集

**`desktop-app` 集成：**
- `build.gradle.kts` 添加 `:ui:designsystem` 依赖
- `Main.kt` 中的 `MaterialTheme` 替换为 `EasyAdbTheme`
- 窗口文本更新为 "EasyADB CMP v2.0.0 - P2 Theme ready"

### 关键设计决策

- **高 DPI**：Compose + Skia 自动处理，删除 Python 中所有手动缩放代码（`UiUtils.getScaleValue` 等）
- **日志颜色**：`LogColor.byLevel(level: Int)` 替代 Python 的 `changeLogColor` 返回 HTML `<font>` 字符串，UI 层根据 level 取 `Color`
- **图标**：用 Material Icons 替代 PNG 图标文件，保证三端矢量清晰；root/unroot 等无 Material 等效的图标后续用 SVG 自定义

### 验收清单

- [x] `./gradlew :ui:designsystem:build` — 设计系统模块独立编译通过
- [x] `./gradlew build` — BUILD SUCCESSFUL
- [x] `MaterialTheme` 替换为 `EasyAdbTheme`
- [x] 字体按平台 fallback（Windows/macOS/Linux）

### 新增坑点

- **Kotlin 块注释嵌套陷阱**：`/*  */` 出现在 KDoc 中时会被 Kotlin 解释为嵌套块注释开始，注意转义或改写描述文本
- **`compose.materialIconsExtended`**：使用 `Icons.Filled.Help`/`Terminal`/`PhoneAndroid` 等扩展图标需要额外添加此依赖，核心集只包含约 900 个图标

---

## P3 — 主窗口骨架 ✅

> 完成日期：2026-07-01 | 分支：`dev/v2.0.0_cmp`

### 完成的工作

**`ui/home` 模块创建：**

- **`MainWindowScreen.kt`** — 主窗口三栏布局：
  ```
  Column
  ├── CustomMenuBar（自定义应用内菜单栏，含 DropdownMenu）
  ├── ToolBar（新建设备连接 / 打开 Shell / Root / Unroot）
  ├── SplitPane（水平拖拽分割）
  │     ├── DeviceListPanel（左 20%，占位 Box）
  │     └── FunctionPanel（右 80%，占位 Box）
  └── BottomTabHost（底部可折叠 Tab 区）
        ├── 控制台（占位）
        └── 实时日志（占位）
  ```
- **`components/SplitPane.kt`** — 可拖拽水平分割面板，使用 `BoxWithConstraints` + `detectDragGestures`，支持最小宽度限制
- **`components/BottomTabHost.kt`** — 可折叠/展开底部 Tab（点击当前激活 Tab 折叠，点击其他 Tab 切换并展开），使用 `AnimatedVisibility` 动画

**`desktop-app` 集成：**
- `build.gradle.kts` 添加 `:ui:home` 依赖
- `Main.kt` 从 XML 加载菜单配置（`menus_ui.xml`），传入 `MainWindowScreen`
- 工具栏操作（AddDevice/OpenShell/Root/Unroot）已接入框架，P3 阶段操作记录日志
- 窗口文本更新为 "EasyADB CMP v2.0.0 - P3 Main Window active"

### 关键设计决策

- **自定义菜单栏替代原生菜单栏**：`FrameWindowScope.menuBar` / `Menu` / `Item` 在 Compose Multiplatform 1.7.3 中为 `compose.desktop.currentOs` 内部成员函数（non-top-level），无法从独立模块导入调用。改为 `CustomMenuBar` + `DropdownMenu` 实现应用内菜单栏，跨平台兼容性更好。
- **`AppIcons.Add` 属性名修正**：`AppIcons` 中属性名为 `AddNew` 而非 `Add`（对应 `Icons.Filled.Add`）。
- **占位内容**：所有面板（DeviceList/Function/Console/Logcat）目前为占位 `Box` + `Text`，P4-P6 逐步替换为实际实现。

### 验收清单

- [x] `./gradlew :ui:home:build` — home 模块独立编译通过
- [x] `./gradlew build` — BUILD SUCCESSFUL
- [x] 三栏布局可见（设备列表 + 分割条 + 功能面板）
- [x] 分割条可拖拽调整比例
- [x] 底部 Tab（控制台/实时日志）可切换和折叠/展开
- [x] 自定义菜单栏从 XML 加载，菜单项可点击弹出 DropdownMenu
- [x] 工具栏 4 个按钮（新建设备连接/Shell/Root/Unroot）就位
- [x] 启动日志输出 "P3 Main Window active"

### 新增坑点

- **`AppIcons` 属性名**：`Icons.Filled.Add` 映射为 `AppIcons.AddNew`，不是 `Add`
- **`::class.java` 不可用于顶层函数**：`MainWindowScreen` 是顶层函数而非类，`MainWindowScreen::class.java` 编译错误，顶层 logger 用 `::class.java` 需确保目标为类
- **`BoxWithConstraints` 导入**：需显式 import `androidx.compose.foundation.layout.BoxWithConstraints`

```bash
cd /e/GitHubProjects/EasyPyADB/cmp
export JAVA_HOME="/d/jdks/jdk-17.0.13"
./gradlew build        # 构建全部
./gradlew run          # 运行应用
./gradlew :core:adb:build   # 只编译特定模块
```

---

## P4 — 设备树 + 命令树 ✅

> 完成日期：2026-07-03 | 分支：`dev/v2.0.0_cmp`

### 完成的工作

**`ui/devicelist` 模块创建（3 文件）：**

- **`TreeNode.kt`** — sealed class 替代 Python QStandardItem，6 种节点类型：
  - `DeviceGroupRootNode` / `CommandGroupRootNode`（顶层根节点，可双击折叠）
  - `DeviceNode`（设备，含 `displayText`/`isOnline` 派生属性）
  - `CommandGroupNode` / `CommandSubGroupNode` / `CommandNode`（命令树三层）
  - `buildDeviceNodes()` / `buildCommandNodes()` 构造器
- **`TreeItem.kt`** — 单行节点视图，支持展开箭头/状态图标/选中态/hover 态
- **`DeviceListPanel.kt`** — 整个左侧面板入口，对齐 Python `MainWindow.init_tree_view`：
  - 顶部 `SectionHeader("功能区")`，对应 Python `setHeaderData`
  - **单一 `LazyColumn`**（设备+命令共享垂直滚动）
  - 设备根节点 / 命令根节点支持双击折叠
  - 折叠状态用 `mutableStateMapOf<String, Boolean>` 按 id 持久化

**`ui/designsystem` 模块扩展（2 文件）：**

- **`AppImages.kt`** — PNG 图片资源访问器：
  - `deviceConnected()` / `deviceOffline()` / `deviceDisconnected()` 三个 `@Composable` 返回 `Painter`
  - 资源目录：`ui/designsystem/src/main/resources/img/`
- **图片迁移**：`res/img/state_connect_normal.png` / `state_connect_offline.png` / `state_disconnect.png` → `cmp/ui/designsystem/src/main/resources/img/`

**`ui/home/MainWindowScreen.kt` 集成：**

- 引入 `DeviceListPanel` 替换占位 Box
- 新增参数：`dbDevices`、`onlineDevices`、`cmdGroups`、`selectedDeviceIp`、`deviceListCallbacks`

**`desktop-app/Main.kt` 集成：**

- 新增 `loadCmdConfigFromClasspath()` + `parseCmdConfigXml()` 加载 cmdConfig.xml
- `watcher.devices.collectAsState()` + `dbDevicesFlow` 双数据源驱动 UI
- `DeviceListCallbacks` 接通：单击选中、双击连接、删除设备等

### 性能优化（关键）

| 优化点 | 解决的问题 |
|---|---|
| 叶子节点用 `Modifier.clickable` 替代 `detectTapGestures(onTap, onDoubleTap)` | 消除 ~200ms doubleTapTimeout 等待 — **点击即时响应** |
| 分组节点保留 `pointerInput + detectTapGestures` 双击折叠 | 分组数量少，延迟可接受 |
| `painterResource` 通过 `DeviceStateIcons` 在 LazyColumn 外层缓存注入 | 避免每行重复加载 PNG |
| `StateFlow` 自动内容去重（`DeviceInfo` 是 data class） | 设备状态没变时不触发 UI 重组 |
| 删除 `SectionHeader` 中空操作的 `pointerInput` | 减少无意义手势识别开销 |

### 关键设计决策

- **sealed class 替代 QStandardItem**：6 种节点类型编译期穷举，比 Python 用 `type()` 字段判断更安全
- **图片资源必须迁移**：Material Design 化只针对颜色，PNG 图片必须保留（参见 `material_rule_scope.md`）
- **单击/双击分层处理**：`supportsDoubleClick` 参数区分叶子节点（零延迟单击）和分组节点（保留双击折叠）
- **资源加载走 classpath**：`config/cmdConfig.xml` 与 `config/menus_ui.xml` 一致从 ClassLoader 加载，不受工作目录影响

### 验收清单

- [x] `./gradlew :ui:devicelist:build` — 模块独立编译通过
- [x] `./gradlew build` — BUILD SUCCESSFUL
- [x] 启动后看到设备列表（首次从老 `easyADB.db` 自动加载 20 个设备）
- [x] 设备节点显示状态图标（PNG 图片，对齐 Python 视觉）
- [x] 命令树 5 个分组（Packages/Display/System/Network/About）就位
- [x] 单击设备/命令项零延迟响应（无 200ms 双击等待）
- [x] 双击"设备列表"/"命令列表"根节点可折叠
- [x] 双击分组、子分组可折叠
- [x] 整个树共用单一垂直滚动条（对齐 Python `QTreeView`）

### 新增坑点

- **`StateFlow.distinctUntilChanged()` 是空操作**：StateFlow 本身已按 `==` 去重，调用该 operator 会被 deprecated 警告
- **`detectTapGestures(onTap + onDoubleTap)` 必有 ~200ms 延迟**：内部需等待 doubleTapTimeout 区分单/双击；纯叶子节点应改用 `Modifier.clickable`
- **`rememberRipple` 已 deprecated**：Compose Multiplatform 1.7.3 中需用新 ripple API；如果项目 allWarningsAsErrors，需直接传 `indication = null`
- **`pointerInput(key)` 的 key 选择**：用 `node.id` 保证节点复用时手势识别重启；用 `Unit` 则不会重启（按需选择）

---

## 后续阶段模板

> 每个阶段完成后，将 `⏳ 未开始` → `✅ 已完成`，填写完成日期和提交，并在下方追加该阶段的详细记录。

### 记录格式

```
## P{N} — {阶段名称} ✅

> 完成日期：{YYYY-MM-DD} | 提交：`{commit-hash}` | 分支：`dev/v2.0.0_cmp`

### 完成的工作
- {关键交付物清单}

### 验收清单
- [ ] {验收项 1}
- [ ] {验收项 2}

### 新增坑点
- {本阶段发现的注意事项}
```
